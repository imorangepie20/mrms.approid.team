"use client";
import { useState } from "react";
import { filterCatalogTracks } from "@/lib/music/catalog";
import { catalog } from "@/lib/music/fixtures";
import { useMusicSession } from "@/providers/music-session-provider";
export function EmsBrowser({ autoFocus = false }: { autoFocus?: boolean }) { const [query, setQuery] = useState(""); const tracks = filterCatalogTracks(catalog, { query }); const { playTrack } = useMusicSession(); return <section className="dashboard-page"><header className="space-title">External Music Space <small>EMS</small></header><div className="space-hero ems-hero"><p>EXTERNAL MUSIC SPACE</p><h1>EMS</h1><span>외부 플랫폼에서 모인 트랙과 플레이리스트 카탈로그입니다.</span><strong>{tracks.length}<small>총 트랙</small></strong></div><div className="filter-row"><button>전체</button><button>Tidal</button><button>Spotify</button><button>Apple Music</button><input autoFocus={autoFocus} aria-label="카탈로그 검색" placeholder="검색" value={query} onChange={(e) => setQuery(e.target.value)} /></div><div className="track-table"><h2>트랙 목록 <small>{tracks.length}곡</small></h2>{tracks.map((track, i) => <div className="track-row" key={track.id}><i>{i + 1}</i><div className={`cover bg-gradient-to-br ${track.artworkClass}`} /><b>{track.title}<small>{track.artist}</small></b><span>{track.album}</span><em>Tidal</em><button onClick={() => playTrack(track)}>▶</button></div>)}</div></section>; }
