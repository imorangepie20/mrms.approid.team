import { useEffect, useState } from "react";
import { Check, RotateCcw, Save } from "lucide-react";
import { getEmsSections, updateEmsSection, type EmsSection } from "../lib/api";
import { apiErrorMessage } from "../lib/ui";

type Draft = Pick<EmsSection, "title" | "description" | "sortOrder" | "active">;

export default function Sections() {
  const [sections, setSections] = useState<EmsSection[]>([]);
  const [drafts, setDrafts] = useState<Record<string, Draft>>({});
  const [saving, setSaving] = useState<string | null>(null);
  const [notice, setNotice] = useState("");
  const [error, setError] = useState("");
  useEffect(() => { void getEmsSections().then((items) => { setSections(items); setDrafts(Object.fromEntries(items.map((item) => [item.id, { title: item.title, description: item.description, sortOrder: item.sortOrder, active: item.active }]))); }).catch((reason: unknown) => setError(apiErrorMessage(reason))); }, []);
  const setDraft = (id: string, patch: Partial<Draft>) => setDrafts((current) => ({ ...current, [id]: { ...current[id], ...patch } }));
  const resetDraft = (section: EmsSection) => setDrafts((current) => ({ ...current, [section.id]: { title: section.title, description: section.description, sortOrder: section.sortOrder, active: section.active } }));
  const save = async (section: EmsSection) => { const draft = drafts[section.id]; if (!draft) return; setSaving(section.id); setNotice(""); setError(""); try { const saved = await updateEmsSection(section.id, draft); setSections((current) => current.map((item) => item.id === saved.id ? saved : item)); setNotice(`${saved.title} 저장 완료`); } catch (reason) { setError(apiErrorMessage(reason)); } finally { setSaving(null); } };
  return <div className="space-y-6"><section><p className="text-xs uppercase tracking-[0.28em] text-hud-accent-primary">EMS / Sections</p><h1 className="mt-2 text-3xl font-semibold">카테고리 관리</h1><p className="mt-2 text-sm text-hud-text-secondary">메인과 EMS에 노출되는 카테고리 메타데이터를 수정합니다.</p></section>{(error || notice) && <div role={error ? "alert" : "status"} className={`rounded-xl border px-4 py-3 text-sm ${error ? "border-hud-accent-danger/40 bg-hud-accent-danger/10 text-hud-accent-danger" : "border-hud-accent-success/40 bg-hud-accent-success/10 text-hud-accent-success"}`}>{error || notice}</div>}<div className="space-y-4">{sections.map((section) => { const draft = drafts[section.id]; return <article className="hud-card rounded-2xl p-5" key={section.id}><div className="mb-5 flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs uppercase tracking-wider text-hud-text-muted">{section.slug}</p><p className="mt-1 text-sm text-hud-text-secondary">{section.trackCount} tracks · {new Date(section.updatedAt).toLocaleDateString("ko-KR")}</p></div><label className="flex items-center gap-2 text-sm text-hud-text-secondary"><input checked={draft?.active ?? section.active} onChange={(event) => setDraft(section.id, { active: event.target.checked })} type="checkbox" /> 활성</label></div><div className="grid gap-4 lg:grid-cols-[1fr_1.6fr_100px_auto]"><label className="text-sm text-hud-text-secondary">제목<input className="mt-2 w-full rounded-lg border border-hud-border-secondary bg-hud-bg-primary px-3 py-2 text-hud-text-primary" value={draft?.title ?? section.title} onChange={(event) => setDraft(section.id, { title: event.target.value })} /></label><label className="text-sm text-hud-text-secondary">설명<input className="mt-2 w-full rounded-lg border border-hud-border-secondary bg-hud-bg-primary px-3 py-2 text-hud-text-primary" value={draft?.description ?? section.description} onChange={(event) => setDraft(section.id, { description: event.target.value })} /></label><label className="text-sm text-hud-text-secondary">순서<input className="mt-2 w-full rounded-lg border border-hud-border-secondary bg-hud-bg-primary px-3 py-2 text-hud-text-primary" min="0" type="number" value={draft?.sortOrder ?? section.sortOrder} onChange={(event) => setDraft(section.id, { sortOrder: Number(event.target.value) })} /></label><div className="mt-auto flex gap-2"><button aria-label={`${section.title} 변경 취소`} className="inline-flex h-10 items-center justify-center gap-2 rounded-lg border border-hud-border-secondary px-3 text-sm text-hud-text-secondary hover:text-hud-text-primary disabled:opacity-50" disabled={saving === section.id} onClick={() => resetDraft(section)} type="button"><RotateCcw size={16} /> 취소</button><button className="inline-flex h-10 items-center justify-center gap-2 rounded-lg bg-hud-accent-primary px-4 text-sm font-semibold text-hud-bg-primary disabled:opacity-50" disabled={saving === section.id} onClick={() => void save(section)} type="button">{saving === section.id ? <Check size={16} /> : <Save size={16} />} 저장</button></div></div></article>; })}</div></div>;
}
