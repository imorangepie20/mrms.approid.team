# music-pie 작업 규칙

## 작업 전 읽기

1. `docs/project-rules/core-principles.md`
2. `docs/project-rules/doc-access-order.md`
3. `docs/overview/current-development-context.md`
4. `docs/overview/project-brief.md`

프로젝트 개요 → 관련 설계·업무 흐름 → 변경 기록 → 구현·테스트 순서로 조사한다. 하위 `AGENTS.md`가 있으면 해당 경로의 지침도 적용한다. 문서가 없으면 기존 대체 문서를 찾아 사용한다.

## 프로젝트 경계

- 목표는 TIDAL API로 사용자의 플레이리스트를 받아 음악 취향을 분석하고 맞춤 음악을 추천하는 것이다.
- TIDAL 접근 권한은 확보된 것으로 본다. 기술 스택, 실제 인증 범위와 데이터 보관 정책은 확인 전에는 추정하여 구현·배포·외부 연동하지 않는다.
- 다른 프로젝트·DB·계정·배포 대상의 코드, 데이터, 비밀값을 혼용하지 않는다.
- 사용자 변경과 요청 범위 밖 코드를 되돌리지 않는다.
- 비밀값·쿠키·캐시·DB dump는 저장소에 넣지 않는다.

## 도메인 원칙

- 데이터 관계, 상태 전이, 권한 규칙과 최종 상태를 확정하는 시스템은 구현과 기존 문서에서 확인한 근거로만 정한다.
- 플레이리스트 접근은 사용자가 허용한 TIDAL API 권한 범위 안에서만 수행하며, 받은 데이터의 저장·삭제 정책은 명시한 뒤 적용한다.
- 추천 결과는 사용자에게 제안하는 정보이며, 취향 분석의 입력과 기준을 추적 가능하게 기록한다.
- 사용자별 취향 모델은 해당 사용자의 플레이리스트와 추천 결과에 대한 액션을 근거로 갱신한다. 다른 사용자의 개인 모델·행동 데이터를 혼용하지 않는다.
- 사용자가 좋아하지 않음으로 판정한 트랙은 해당 사용자의 이후 추천 후보에서 영구 제외한다. 이 제외 규칙은 다른 사용자의 추천과 EMS 원본 카탈로그에는 영향을 주지 않는다.
- AI 기능의 허용 범위는 해당 기능이 확인된 뒤 명시한다.

## 구현과 검증

- 설계·구현 계획·완료 기준을 먼저 기록한다. 작은 변경은 짧은 기록으로 충분하다.
- 다단계 작업은 변경 파일·의존 순서·검증 방법을 `docs/plans/`에 기록한다.
- 버그는 원인을 조사하고 의미 있는 재현·회귀 검증을 수행한다.
- 동작 변경은 관련 실패 테스트 또는 동등한 재현 근거를 확보한다.
- 직접 테스트와 필요한 타입·빌드 검사를 우선한다.
- 전체 테스트·브라우저 회귀는 사용자 요청 또는 변경 영향이 요구할 때 실행한다.
- UI는 기존 기능성 컴포넌트를 우선하고 키보드·모바일 동작을 확인한다.
- 외부 환경 변경은 사용자가 요청한 범위와 현재 권한을 따른다.
- diff에서 요청 범위 밖 변경과 권한·상태·오류 경계 누락을 검토한다.
- 파일이나 명령의 존재만으로 기능이 완료됐다고 보고하지 않는다.

## 위임과 확장

- 독립적인 하위 작업이 있고 현재 지침·사용자 요청이 허용할 때만 위임한다.
- 스킬·플러그인은 선택 확장이다. 없어도 `docs/harness/portable-project-harness.md` 12절의 기본 절차를 직접 수행한다.
- 스킬·플러그인의 설치·세션 노출·실행 상태는 `docs/harness/skill-plugin-environment.md`에 구분해서 기록한다.

## 문서화

- 문서는 한국어로 작성하고 경로·명령·식별자는 원문을 유지한다.
- 변경 이유·내용·검증 결과·미검증 항목·다음 작업을 기록한다.
- 실제로 성공한 명령만 검증 완료로 표시한다.
